# 工程协作约定

适用于本仓库的开发、审查与维护。用户当前明确要求优先；本文件不覆盖系统或开发者指令。规则描述必须守住的行为，源码说明当前实现；两者不符时定位差异，不能把现有缺陷当成放宽规则的理由。

## 先确定工作范围

修改目录前读取该路径上的 `AGENTS.md`；根目录启动的客户端不一定自动加载子目录文件，按下表补读。只加载本次涉及的内容，跨模块任务覆盖双方。

| 工作范围 | 必读入口 |
|---|---|
| Web 页面、组件、Store | [web/AGENTS.md](web/AGENTS.md) |
| Backend API、SQLite、媒体、执行器 | [backend/AGENTS.md](backend/AGENTS.md) |
| Agent 会话、MCP 工具、共享契约 | [canvas-agent/AGENTS.md](canvas-agent/AGENTS.md) |
| 画布插件、SDK、H3 UI | [plugins/canvas/AGENTS.md](plugins/canvas/AGENTS.md) |
| 画布持久编辑、同步、生成、参考或任务回写 | [画布数据契约](.agents/rules/canvas-contracts.md)，与代码目录无关 |
| 画布主题、视口、拖拽、H3 布局 | [画布交互约束](.agents/rules/canvas-ui.md) |
| H3 短片操作、补段、返修 | [生产操作边界](.agents/rules/h3-production.md)，再按当前阶段读生产 SOP |
| 用户可感知变更或文档修改 | [docs/AGENTS.md](docs/AGENTS.md) |
| PR 处理、提交、发版本 | [交付流程](.agents/rules/delivery.md) |
| 产物、加载路径、启动与重载问题 | [运行与验证](.agents/skills/canvas-h3-implementation/references/development-verification.md) |

`canvas-agent/agent-instructions.md` 是产品内 Canvas Agent 的运行时提示词，不是本仓库开发约定。工程规则维护不得顺带改变它或覆盖用户运行工作空间的指令。

## 工程职责

- `backend` 是业务数据与任务的权威：SQLite、媒体归档、ops 事务、任务执行、共享 HTTP MCP。
- `canvas-agent` 提供对话运行时、工具与共享契约；Backend 通过该包导出的 `dist` 消费它。
- `web` 提供界面、用户编辑和浏览器执行器；`plugins/canvas` 通过 SDK 接入宿主。画布生成由 Backend 统一建任务。
- npm workspaces 为 `backend`、`canvas-agent`、`web`；文档和插件按各自 package 配置执行。依赖版本、命令和模型能力以当前代码与运行配置为准。

## 执行方式

1. 先查看相关现状和未提交差异，确定本次可观察结果。未知位置或跨文件关系优先用已有 CodeGraph；索引不可用、过期或无关时转 `rg`，不自行建索引。
2. 沿现有入口把目标做完整：数据契约、读写链、消费方和必要文档一起处理。局部任务不全库扫读，不借修复扩张产品范围。
3. 选择最直接、可维护的实现。复用成熟库与项目入口；不为少几行牺牲清晰度，不加无实际调用方的抽象、透传组件或推测性兼容。已有用户数据必须按迁移规则保护。
4. 失败后收集能区分原因的新证据再修。连续尝试仍无改善时换验证方法，不盲目重跑、重复提交收费任务或以固定尝试次数直接放弃。
5. 做完必要验证和交付准备再结束；报告结果、证据及实际剩余限制。区分已写入、已构建、已加载、已运行、已验收，不把推断说成实测。

## 数据与现场保护

- 保留工作区已有修改；不回滚、不覆盖、不用清库、删媒体或重建用户项目掩盖错误。冲突落在同一段时先读最新内容再合并。
- 业务数据归 Backend；未确认 outbox 和冲突草稿也是用户数据，不是可丢弃缓存。数据库与存储格式变更先备份，未知版本或冲突拒绝覆盖。
- 识别实际端点、PID、启动方式和消费产物后再处理服务。`dev` 不代表 watch；`dev:local` 会清理旧服务，不是探活命令。重载只影响必要且已授权的服务，不批量杀进程。
- 浏览器验证使用独立页面；不关闭用户标签页或中断对话。诊断避免输出密钥、完整私有配置和大段媒体内容。

## 自主决策与确认边界

在已授权目标内直接完成调查、实现、可逆修复和相关验证；已有明确授权不重复询问。以下动作需要对应的明确授权，先准备可审查结果再确认：

- 合并进 `main`、关闭 PR、force-push 作者分支；具体要求见交付流程。
- 扩大破坏性操作范围，或改变已有用户数据、媒体和服务的处置方式。
- 新增或调整产品超时、重试、大小限制、并发上限：说明适用环节、默认值及失败处理。用户已指定的值不再确认；一次性诊断等待不属于产品边界。

权限阻塞时继续完成独立工作，准确说明受阻动作；不能用换入口、直写库或扩大权限绕过边界。

## 验证做到恰好充分

| 变更 | 必要验证 |
|---|---|
| 指引、注释、纯文案 | 差异、引用、语言与格式检查 |
| 业务逻辑、API、状态转换 | 相关现有测试；缺少覆盖时补真实失败场景，不写复述实现的测试 |
| 数据迁移、幂等、同步、任务归属 | 临时数据验证回执、重放、冲突、恢复或拒绝路径中受影响的行为 |
| UI 交互、画布性能 | 实际消费页面上的相关行为；画布细则见交互约束 |
| 被 dist/bundle 消费的源码 | 重建受影响产物，并确认实际消费路径 |

默认不执行全仓 typecheck/test/build。一次相关检查通过后，不无理由扩大或重复；发布流程的验证例外见交付流程。不能执行的验证如实列出，不称为通过。

## 指引如何维护

- 一条规则只有一个详细归属：全局边界放本文件；目录约定放对应 `AGENTS.md`；跨模块不变量放 `.agents/rules/`；Skill 写任务方法与验证。
- 重复提醒或已证实的重复故障，先更新所属规则，合并旧条目；写清触发条件、必须保持的行为及验证证据。
- 不沉淀聊天原话、一次性事故叙事、机器绝对路径、行号、过期性能数字或“本会话最高优先级”。能从源码直接查的常量不复制成多份配置。
- 架构变化时同时更新直接关联指引，删除失效入口；不能只加一句新规则而保留相反旧指令。
