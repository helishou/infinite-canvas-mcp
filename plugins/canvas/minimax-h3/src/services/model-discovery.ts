import type { CanvasNodeContext } from "@infinite-canvas/plugin-sdk";
export type H3ModelCatalog = { models: string[]; loras: string[]; textEncoders: string[]; videoVaes: string[]; audioVaes: string[]; latentUpscaleModels: string[]; nanfeng: Record<string, unknown[]> };
export function discoverH3Models(ctx: CanvasNodeContext): Promise<H3ModelCatalog> { return ctx.ai.listLocalH3Models().then((value) => ({ models: value.models || [], loras: value.loras || [], textEncoders: value.textEncoders || [], videoVaes: value.videoVaes || [], audioVaes: value.audioVaes || [], latentUpscaleModels: value.latentUpscaleModels || [], nanfeng: value.nanfeng || {} })).catch(() => ({ models: [], loras: [], textEncoders: [], videoVaes: [], audioVaes: [], latentUpscaleModels: [], nanfeng: {} })); }
export function mergeH3Options<T extends { value: string }>(builtIns: T[], discovered: string[], label = (value: string) => value.replace(/^.*[\\/]/, "")) { return [...builtIns, ...discovered.filter((value) => !builtIns.some((option) => option.value === value)).map((value) => ({ value, label: label(value) }))]; }
